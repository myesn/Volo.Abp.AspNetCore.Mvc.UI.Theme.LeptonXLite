import { IfReplaceableTemplateExistsDirective } from './if-replaceable-template-exists.directive';

describe('IfReplaceableTemplateExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new IfReplaceableTemplateExistsDirective();
    expect(directive).toBeTruthy();
  });
});
