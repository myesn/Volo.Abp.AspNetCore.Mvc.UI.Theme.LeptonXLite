import { InjectionToken } from '@angular/core';
import { LpxPerfectScrollbar } from '../abstracts';

export const LPX_PERFECT_SCROLLBAR = new InjectionToken<LpxPerfectScrollbar>(
  'LPX_PERFECT_SCROLLBAR'
);
